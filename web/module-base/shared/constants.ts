/**
 * Shared constants used across client and server
 */

import { CONFIG } from "../server/config";

export const SYSTEM_TIMEZONE = CONFIG.system.timezone;
export const DEFAULT_TIMEZONE = CONFIG.system.defaultTimezone;
